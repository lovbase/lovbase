# Railway: app built from the root Dockerfile, with Postgres and Redis services alongside it.
# Set DATABASE_URL and REDIS_URL to their private URLs. The sandbox stays on Cloudflare.
[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
